"""Pinpoint.

``__version__`` is the single source of truth for the version shown in the UI.
Commitizen rewrites it on ``cz bump`` -- see ``version_files`` in ``.cz.yaml``.
"""

__version__ = "0.2.0"
