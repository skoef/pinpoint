services:
  app:
    image: docker.io/rschoof/speurtocht:v0.0.0
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY
      - DEBUG
      - ALLOWED_HOSTS
      - USE_S3
      - AWS_STORAGE_BUCKET_NAME
      - AWS_S3_ENDPOINT_URL
      - AWS_ACCESS_KEY_ID
      - AWS_SECRET_ACCESS_KEY
